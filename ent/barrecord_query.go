// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/nathanhack/sibyl/ent/bargroup"
	"github.com/nathanhack/sibyl/ent/barrecord"
	"github.com/nathanhack/sibyl/ent/predicate"
)

// BarRecordQuery is the builder for querying BarRecord entities.
type BarRecordQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	inters     []Interceptor
	predicates []predicate.BarRecord
	withGroup  *BarGroupQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BarRecordQuery builder.
func (brq *BarRecordQuery) Where(ps ...predicate.BarRecord) *BarRecordQuery {
	brq.predicates = append(brq.predicates, ps...)
	return brq
}

// Limit the number of records to be returned by this query.
func (brq *BarRecordQuery) Limit(limit int) *BarRecordQuery {
	brq.limit = &limit
	return brq
}

// Offset to start from.
func (brq *BarRecordQuery) Offset(offset int) *BarRecordQuery {
	brq.offset = &offset
	return brq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (brq *BarRecordQuery) Unique(unique bool) *BarRecordQuery {
	brq.unique = &unique
	return brq
}

// Order specifies how the records should be ordered.
func (brq *BarRecordQuery) Order(o ...OrderFunc) *BarRecordQuery {
	brq.order = append(brq.order, o...)
	return brq
}

// QueryGroup chains the current query on the "group" edge.
func (brq *BarRecordQuery) QueryGroup() *BarGroupQuery {
	query := (&BarGroupClient{config: brq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := brq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := brq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(barrecord.Table, barrecord.FieldID, selector),
			sqlgraph.To(bargroup.Table, bargroup.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, barrecord.GroupTable, barrecord.GroupColumn),
		)
		fromU = sqlgraph.SetNeighbors(brq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first BarRecord entity from the query.
// Returns a *NotFoundError when no BarRecord was found.
func (brq *BarRecordQuery) First(ctx context.Context) (*BarRecord, error) {
	nodes, err := brq.Limit(1).All(newQueryContext(ctx, TypeBarRecord, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{barrecord.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (brq *BarRecordQuery) FirstX(ctx context.Context) *BarRecord {
	node, err := brq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BarRecord ID from the query.
// Returns a *NotFoundError when no BarRecord ID was found.
func (brq *BarRecordQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = brq.Limit(1).IDs(newQueryContext(ctx, TypeBarRecord, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{barrecord.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (brq *BarRecordQuery) FirstIDX(ctx context.Context) int {
	id, err := brq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BarRecord entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BarRecord entity is found.
// Returns a *NotFoundError when no BarRecord entities are found.
func (brq *BarRecordQuery) Only(ctx context.Context) (*BarRecord, error) {
	nodes, err := brq.Limit(2).All(newQueryContext(ctx, TypeBarRecord, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{barrecord.Label}
	default:
		return nil, &NotSingularError{barrecord.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (brq *BarRecordQuery) OnlyX(ctx context.Context) *BarRecord {
	node, err := brq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BarRecord ID in the query.
// Returns a *NotSingularError when more than one BarRecord ID is found.
// Returns a *NotFoundError when no entities are found.
func (brq *BarRecordQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = brq.Limit(2).IDs(newQueryContext(ctx, TypeBarRecord, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{barrecord.Label}
	default:
		err = &NotSingularError{barrecord.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (brq *BarRecordQuery) OnlyIDX(ctx context.Context) int {
	id, err := brq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BarRecords.
func (brq *BarRecordQuery) All(ctx context.Context) ([]*BarRecord, error) {
	ctx = newQueryContext(ctx, TypeBarRecord, "All")
	if err := brq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BarRecord, *BarRecordQuery]()
	return withInterceptors[[]*BarRecord](ctx, brq, qr, brq.inters)
}

// AllX is like All, but panics if an error occurs.
func (brq *BarRecordQuery) AllX(ctx context.Context) []*BarRecord {
	nodes, err := brq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BarRecord IDs.
func (brq *BarRecordQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	ctx = newQueryContext(ctx, TypeBarRecord, "IDs")
	if err := brq.Select(barrecord.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (brq *BarRecordQuery) IDsX(ctx context.Context) []int {
	ids, err := brq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (brq *BarRecordQuery) Count(ctx context.Context) (int, error) {
	ctx = newQueryContext(ctx, TypeBarRecord, "Count")
	if err := brq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, brq, querierCount[*BarRecordQuery](), brq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (brq *BarRecordQuery) CountX(ctx context.Context) int {
	count, err := brq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (brq *BarRecordQuery) Exist(ctx context.Context) (bool, error) {
	ctx = newQueryContext(ctx, TypeBarRecord, "Exist")
	switch _, err := brq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (brq *BarRecordQuery) ExistX(ctx context.Context) bool {
	exist, err := brq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BarRecordQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (brq *BarRecordQuery) Clone() *BarRecordQuery {
	if brq == nil {
		return nil
	}
	return &BarRecordQuery{
		config:     brq.config,
		limit:      brq.limit,
		offset:     brq.offset,
		order:      append([]OrderFunc{}, brq.order...),
		inters:     append([]Interceptor{}, brq.inters...),
		predicates: append([]predicate.BarRecord{}, brq.predicates...),
		withGroup:  brq.withGroup.Clone(),
		// clone intermediate query.
		sql:    brq.sql.Clone(),
		path:   brq.path,
		unique: brq.unique,
	}
}

// WithGroup tells the query-builder to eager-load the nodes that are connected to
// the "group" edge. The optional arguments are used to configure the query builder of the edge.
func (brq *BarRecordQuery) WithGroup(opts ...func(*BarGroupQuery)) *BarRecordQuery {
	query := (&BarGroupClient{config: brq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	brq.withGroup = query
	return brq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Close float64 `json:"close,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BarRecord.Query().
//		GroupBy(barrecord.FieldClose).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (brq *BarRecordQuery) GroupBy(field string, fields ...string) *BarRecordGroupBy {
	brq.fields = append([]string{field}, fields...)
	grbuild := &BarRecordGroupBy{build: brq}
	grbuild.flds = &brq.fields
	grbuild.label = barrecord.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Close float64 `json:"close,omitempty"`
//	}
//
//	client.BarRecord.Query().
//		Select(barrecord.FieldClose).
//		Scan(ctx, &v)
func (brq *BarRecordQuery) Select(fields ...string) *BarRecordSelect {
	brq.fields = append(brq.fields, fields...)
	sbuild := &BarRecordSelect{BarRecordQuery: brq}
	sbuild.label = barrecord.Label
	sbuild.flds, sbuild.scan = &brq.fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BarRecordSelect configured with the given aggregations.
func (brq *BarRecordQuery) Aggregate(fns ...AggregateFunc) *BarRecordSelect {
	return brq.Select().Aggregate(fns...)
}

func (brq *BarRecordQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range brq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, brq); err != nil {
				return err
			}
		}
	}
	for _, f := range brq.fields {
		if !barrecord.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if brq.path != nil {
		prev, err := brq.path(ctx)
		if err != nil {
			return err
		}
		brq.sql = prev
	}
	return nil
}

func (brq *BarRecordQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BarRecord, error) {
	var (
		nodes       = []*BarRecord{}
		withFKs     = brq.withFKs
		_spec       = brq.querySpec()
		loadedTypes = [1]bool{
			brq.withGroup != nil,
		}
	)
	if brq.withGroup != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, barrecord.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BarRecord).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BarRecord{config: brq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, brq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := brq.withGroup; query != nil {
		if err := brq.loadGroup(ctx, query, nodes, nil,
			func(n *BarRecord, e *BarGroup) { n.Edges.Group = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (brq *BarRecordQuery) loadGroup(ctx context.Context, query *BarGroupQuery, nodes []*BarRecord, init func(*BarRecord), assign func(*BarRecord, *BarGroup)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*BarRecord)
	for i := range nodes {
		if nodes[i].bar_group_records == nil {
			continue
		}
		fk := *nodes[i].bar_group_records
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	query.Where(bargroup.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "bar_group_records" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (brq *BarRecordQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := brq.querySpec()
	_spec.Node.Columns = brq.fields
	if len(brq.fields) > 0 {
		_spec.Unique = brq.unique != nil && *brq.unique
	}
	return sqlgraph.CountNodes(ctx, brq.driver, _spec)
}

func (brq *BarRecordQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   barrecord.Table,
			Columns: barrecord.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: barrecord.FieldID,
			},
		},
		From:   brq.sql,
		Unique: true,
	}
	if unique := brq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := brq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, barrecord.FieldID)
		for i := range fields {
			if fields[i] != barrecord.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := brq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := brq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := brq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := brq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (brq *BarRecordQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(brq.driver.Dialect())
	t1 := builder.Table(barrecord.Table)
	columns := brq.fields
	if len(columns) == 0 {
		columns = barrecord.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if brq.sql != nil {
		selector = brq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if brq.unique != nil && *brq.unique {
		selector.Distinct()
	}
	for _, p := range brq.predicates {
		p(selector)
	}
	for _, p := range brq.order {
		p(selector)
	}
	if offset := brq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := brq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// BarRecordGroupBy is the group-by builder for BarRecord entities.
type BarRecordGroupBy struct {
	selector
	build *BarRecordQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (brgb *BarRecordGroupBy) Aggregate(fns ...AggregateFunc) *BarRecordGroupBy {
	brgb.fns = append(brgb.fns, fns...)
	return brgb
}

// Scan applies the selector query and scans the result into the given value.
func (brgb *BarRecordGroupBy) Scan(ctx context.Context, v any) error {
	ctx = newQueryContext(ctx, TypeBarRecord, "GroupBy")
	if err := brgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BarRecordQuery, *BarRecordGroupBy](ctx, brgb.build, brgb, brgb.build.inters, v)
}

func (brgb *BarRecordGroupBy) sqlScan(ctx context.Context, root *BarRecordQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(brgb.fns))
	for _, fn := range brgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*brgb.flds)+len(brgb.fns))
		for _, f := range *brgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*brgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := brgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BarRecordSelect is the builder for selecting fields of BarRecord entities.
type BarRecordSelect struct {
	*BarRecordQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (brs *BarRecordSelect) Aggregate(fns ...AggregateFunc) *BarRecordSelect {
	brs.fns = append(brs.fns, fns...)
	return brs
}

// Scan applies the selector query and scans the result into the given value.
func (brs *BarRecordSelect) Scan(ctx context.Context, v any) error {
	ctx = newQueryContext(ctx, TypeBarRecord, "Select")
	if err := brs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BarRecordQuery, *BarRecordSelect](ctx, brs.BarRecordQuery, brs, brs.inters, v)
}

func (brs *BarRecordSelect) sqlScan(ctx context.Context, root *BarRecordQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(brs.fns))
	for _, fn := range brs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*brs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := brs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
